apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: ${CLUSTER_NAME}

servers: 1
agents: 2

registries:
  config: |
    mirrors:
      "localhost:5000":
        endpoint:
          - "http://${CLUSTER_NAME}-registry:5000"

ports:
  - port: 8080:80
    nodeFilters:
      - loadbalancer

volumes:
  - volume: ${FLUENT_BIR_DIR}/data/gc-logs:/gc-logs
    nodeFilters:
      - all
  - volume: ${FLUENT_BIR_DIR}/dummy-machine-id:/etc/machine-id
    nodeFilters:
      - all

options:
  k3s:
    extraArgs:
      - arg: "--flannel-backend=none"
        nodeFilters:
          - server:*
      - arg: "--disable-network-policy"
        nodeFilters:
          - server:*
      - arg: "--disable=traefik"
        nodeFilters:
          - server:*
